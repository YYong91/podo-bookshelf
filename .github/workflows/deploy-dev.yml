name: CD (Dev)

on:
  push:
    branches: [develop]

concurrency:
  group: cd-dev
  cancel-in-progress: true

jobs:
  notify:
    name: Notify Dev Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram notification
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          COMMIT_MSG: ${{ github.event.head_commit.message || 'Manual trigger' }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=📦 podo-bookshelf develop 머지 완료 — ${COMMIT_MSG}"

  # TODO: dev 환경 구축 시 아래 주석 해제
  # deploy-backend-dev:
  #   name: Deploy Backend to Fly.io (Dev)
  #   needs: [notify]
  #   runs-on: ubuntu-latest
  #   env:
  #     FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: superfly/flyctl-actions/setup-flyctl@master
  #     - run: flyctl deploy --local-only --config backend/fly.dev.toml --dockerfile backend/Dockerfile

  # deploy-frontend-dev:
  #   name: Deploy Frontend to Cloudflare Pages (Dev)
  #   needs: [notify]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "22"
  #         cache: "npm"
  #         cache-dependency-path: frontend/package-lock.json
  #     - run: cd frontend && npm ci
  #     - run: cd frontend && npm run build
  #       env:
  #         VITE_API_URL: ${{ secrets.VITE_DEV_API_URL }}
  #         VITE_AUTH_URL: ${{ secrets.VITE_AUTH_URL }}
  #         VITE_AUTH_CALLBACK_URL: ${{ secrets.VITE_DEV_AUTH_CALLBACK_URL }}
  #         VITE_BUDGET_URL: ${{ secrets.VITE_BUDGET_URL }}
  #     - run: npx wrangler@3 pages deploy frontend/dist --project-name=podo-bookshelf-dev --branch="develop"
  #       env:
  #         CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  #         CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
