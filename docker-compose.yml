services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    volumes:
      - podo-data:/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////data/podo.db
      - CORS_ORIGINS=*
      - JWT_SECRET=${JWT_SECRET:-podo-jwt-secret-change-in-production}
      - AUTH_SERVER_URL=https://auth.podonest.com
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_AUTH_URL=https://auth.podonest.com
        - VITE_AUTH_CALLBACK_URL=https://bookshelf.podonest.com/auth/callback
        - VITE_BUDGET_URL=https://budget.podonest.com
    ports:
      - "3100:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  podo-data:
